; DDnsClient 設定ファイル
;
; 設定によってはアプリケーションが正常に動作しなくなるかもしれません。
; 変更は慎重に行って下さい。
;
; -----------------------------------------------------------------------------
; URL最大登録件数
20
; -----------------------------------------------------------------------------
; ヘッダフィールド最大登録件数
20
; -----------------------------------------------------------------------------
; 特別なボタンの文字色 (赤=ff0000)
000080
; -----------------------------------------------------------------------------
; 行番号の背景色 (赤=ff0000)
e0ffff
; -----------------------------------------------------------------------------
; この回数連続でエラーが発生したら警告アイコン (赤アイコン) を表示する。
3
; -----------------------------------------------------------------------------
